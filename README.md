# v7petrov.github.io

Web portfolio as an alternative to just a google docs link.

## Resume PDF

The live site displays `assets/resume.pdf` through `resume.html`.

Edit the LaTeX source in `_resume/resume.tex`, render it to PDF locally or in Overleaf, export the result to `assets/resume.pdf`, then push. GitHub Pages serves the committed PDF as a normal static asset.
